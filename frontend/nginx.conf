server {
    listen 80;

    location / {
        root   /usr/share/nginx/html;
        index  index.html;
    }

    location /delayed-notifier/ {
        proxy_pass http://delayed-notifier:8080/;
    }
} 